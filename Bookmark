javascript:(function(){const u="你的_WEB_APP_URL";let s={};document.querySelectorAll(".id").forEach(e=>{let m=e.innerText.match(/CM\s*([A-Za-z0-9-]+)/i);if(m)s[m[1].toUpperCase()]=1});Object.keys(s).length?fetch(u,{method:"POST",body:JSON.stringify(s),mode:"no-cors"}).then(()=>alert("✅ 发送中...\n请在10秒后检查iPad或表格")):alert("❌ 页面未发现货号")})();
